import { createSlice } from "@reduxjs/toolkit";

export const reducer = createSlice({
    name: "Auth",
    initialState: { value: {} },
    reducers: {
        login: (state, action) => {

        }
    }
})
